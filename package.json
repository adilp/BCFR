{
  "name": "@memberorg/root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "mobile": "yarn workspace @memberorg/mobile start",
    "web": "yarn workspace @memberorg/web dev",
    "api": "yarn workspace @memberorg/api dev"
  },
  "dependencies": {
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
